// todo adjust or delete if the bonus is not Profile page
export default {
    count(arr) {
        return arr.length;
    },
};

// use example in the hbs tamplate (Profile.hbs)
// {{coint createdBlogs}}
